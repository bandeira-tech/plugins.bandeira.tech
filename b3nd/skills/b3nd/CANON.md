# CANON — envelopes, content addressing, encryption

> STUB — fill in iteratively.

## Cover

- `b3nd-canon` package: envelope shape, RFC 8785 canonical JSON, `hash://sha256/...` URIs.
- When to use canonical envelopes (signing, content addressing) vs. plain payloads.
- Client-side encryption story: when, how, key custody.
- Practical: how to wire `hash://` into a protocol so binary payloads behave.

## Voice note

This file is dense by nature. Don't introduce all of it at once. Surface only the piece the user actually needs.
